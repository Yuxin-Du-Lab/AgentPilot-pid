# PID 超参数敏感性评估：方法说明

> 配套源码：`pid_exp/`；配套设计文档：`docs/code_design.md`、`docs/experiment_design.md`
> 基线日期：2026-06-06

本文档分两部分：第一部分用工程语言说明这套评估代码**做什么、怎么做**；第二部分用 scientific writing 的方式把它凝练为可直接用于论文的实验方法段落。

---

## 第一部分　方法详解

### 1. 评估目标：这套代码在做什么

这是一个**独立的 PID 超参数敏感性实验台**。它的唯一目的，是定量回答一个问题：

> 当 PID 的三个超参数 $K_p$（比例）、$K_i$（积分）、$K_d$（微分）各自偏离默认值时，飞行器在**单轴阶跃响应（step response）**下的控制质量如何变化——变化的方向、强度与线性度各是多少？

最终交付物是一张**敏感性表**（`sensitivity_table.md`）和两类图（叠加响应曲线、敏感性散点拟合），让读者一眼看出"哪个参数对哪个指标影响最大、是正是负、是否单调"。

它从一个复杂的视觉跟踪 Agent 中**抽离**出纯 PID 控制回路，剥掉了视觉跟踪、VLM 安全检查、MCP 封装等一切无关依赖，只保留"读状态—算控制—发动作"这条最小闭环，并直连真实的 **MSFS 2024** 仿真环境。

### 2. 评估了哪些超参数：OFAT 设计

采用 **OFAT（One-Factor-At-a-Time，一次一因子）** 方法：每次只让一个超参数偏离 nominal（默认值），其余两个固定。每个参数在 $\pm10\%$、$\pm20\%$ 四个档位上扫描，加上公共的 Nominal 原点，单轴共 **13 个配置**：

| 组 | 配置数 | 取值 |
|---|---|---|
| Nominal | 1 | $(K_p^0, K_i^0, K_d^0)$ |
| $K_p$ 扫描 | 4 | $K_p^0\times\{0.8,\,0.9,\,1.1,\,1.2\}$，$K_i,K_d$ 固定 |
| $K_i$ 扫描 | 4 | $K_i^0\times\{0.8,\,0.9,\,1.1,\,1.2\}$，$K_p,K_d$ 固定 |
| $K_d$ 扫描 | 4 | $K_d^0\times\{0.8,\,0.9,\,1.1,\,1.2\}$，$K_p,K_i$ 固定 |

各轴的默认值沿用原 Agent 标定，不重新调参：

| 轴 | $K_p^0$ | $K_i^0$ | $K_d^0$ |
|---|---|---|---|
| heading（航向） | 2.0 | 0.10 | 0.5 |
| altitude（高度） | 1.5 | 0.05 | 0.5 |

**为什么用 OFAT 而非全因子网格**：本研究只关心每个超参数的**独立主效应**，不研究参数间交互项。OFAT 用 13 个配置即可在每个参数上给出 5 点单调曲线，可读性远高于 $3^3$ 或 $5^3$ 全因子网格，时间成本也低一个量级。

**重复实验（replication）**：本系统存在真实随机性——HTTP 节拍抖动、bang-bang 控制的 chattering、复位 $\pm5°$ 容差导致真实阶跃量在 $25°\!-\!35°$ 间浮动。单次结果会误导，因此**每个配置重复 10 次**，单轴共 $13\times10=130$ 个 run。执行顺序**分组**（同一配置的 10 次连续跑完再换下一配置），敏感性拟合建立在每档 10 次的**均值**上，并以标准差呈现重复间散布。

### 3. 如何形成闭环：单次实验的控制回路

单次实验 = 对**一个轴**做一次阶跃响应，由 `experiment.run()` 驱动，时长固定 45 s。闭环由四个环节首尾相接（源码：`experiment.py:88-123`）：

```
   ┌────────────────────── 每个迭代 (~1.1–2.1 s) ──────────────────────┐
   │                                                                    │
   │   ① read_state()          ② pid.update()         ③ 动作映射+执行   │
   │   从 MSFS 读真实状态  ──►  算 error 与控制量  ──►  bang-bang 发舵   │
   │   (heading/altitude)      output=P+I+D            move_xxx(t)      │
   │        ▲                                               │           │
   │        │                                               ▼           │
   │        └────────────── ④ MSFS 物理演化 ◄───────────────┘           │
   │                        (飞机姿态改变)                              │
   └────────────────────────────────────────────────────────────────────┘
                    ↑ 循环直到 t > 45 s，每个迭代写一行 log
```

逐环节说明：

1. **读反馈（`flight_io.read_state`）**：HTTP GET 仿真桥，取 `PLANE_HEADING_DEGREES_MAGNETIC` 与 `PLANE_ALTITUDE`。反馈量是**仿真直接给出的航向/高度真值**，不再依赖原系统的视觉 bbox 跟踪——这是闭环"测量"端的简化与提纯。
   - **单位陷阱**：该 simvar 名字带 "DEGREES"，但 SimConnect 实际收发**弧度**（官方文档明确）。转换被严格封装在 `flight_io` 一层（读时 rad→deg、写时 deg→rad），对上层 PID/metrics 一律以"度"工作。

2. **算控制（`pid_controller.PIDController.update`）**：标准 1D PID，$\text{output}=K_p e + K_i\!\int\! e\,dt + K_d\,\dot e$。`dt` 由调用方传入真实循环间隔（而非内部 `time.time()`）。航向轴的误差经 `normalize_heading_error` 折叠到 $(-180°,180°]$，正确处理 $360°$ wrap-around。

3. **发动作（`experiment._pick_action` + `flight_io.move_xxx`）**：把连续的 PID 输出映射成**离散的 bang-bang 指令**——舵面偏转幅度固定，只调**动作持续时长** $t_\text{action}=\text{clip}(|\text{output}|\times0.5,\;0.1,\;1.0)$ 秒。这是从原系统保留的映射，本身不是研究对象。

4. **物理演化**：`move_xxx` 阻塞执行 $t_\text{action}$ 秒后调用 `hover()` 归中并 `sleep(1)`，飞机姿态在 MSFS 中真实改变；下一迭代 `read_state` 读到新状态，闭环成立。这条**末尾 1 s 的 hover** 是天然阻尼，使系统超调通常不严重。

四步首尾相接构成**测量→决策→执行→被控对象→再测量**的标准反馈闭环；与教科书 PID 的唯一区别是执行端是离散 bang-bang、被控对象是真实仿真而非数学模型。

### 4. 为什么、以及如何解耦两个轴

#### 为什么要解耦

- **方法论要求**：OFAT 测的是单因子对**单个被控变量**的主效应。若航向与高度同时做阶跃、同时被控，两轴的物理耦合（如转弯倾斜带来掉高）会**混淆**到指标里，敏感性表将无法归因到"是 $K_p$ 的作用还是耦合的作用"。
- **可读性与可归因**：解耦后，每个 run 只有一条被控曲线、一组干净的指标，敏感性斜率可直接解释为"该参数对该轴该指标的影响"。
- **尊重原系统结构**：原 Agent 本就是两个独立的 1D PID——`pid_x` 经方向舵控航向、`pid_y` 经升降舵控高度，二者解耦运行。实验台沿用这一结构。

#### 如何实现解耦

解耦不是靠数学投影，而是靠**实验隔离 + 正交执行机构**，落在四个层面：

1. **一次只跑一个轴**：`--axis heading` 与 `--axis altitude` 是两次完全独立的 sweep，各有自己的默认 PID 值、阶跃设置和结果目录。

2. **被动轴冻结**：跑某一轴时，另一轴被 `reset_to` 设到固定值（`other_axis_value`），且**不施加任何控制**——它的舵面在 `hover()` 中保持归中。例如测航向时高度固定 5000 ft、升降舵中立；测高度时航向固定 90°、方向舵中立。

3. **正交执行机构**：动作映射按轴选择**物理上不同的舵面**（`experiment.py:39-50`）——
   - 航向：output 符号 → `hover_turn_right/left`（**方向舵** RUDDER $\pm0.05$）
   - 高度：output 符号 → `move_ascend/descend`（**升降舵** ELEVATOR $\pm1.0$）

   方向舵与升降舵互不干涉，主动轴的舵动作不会直接驱动被动轴。

4. **独立 PID 实例**：`PIDController(axis=...)` 每次只为当前轴建一个 1D 控制器，航向轴自动启用 wrap-around，高度轴不启用。

> 需要说明：真实仿真中仍存在残余物理耦合（气动交叉、姿态联动）。本设计通过"冻结被动轴 + 正交舵面 + 末尾 hover 归中"将其压到最小，并且**指标只统计主动轴**，从而在工程上达成"近似解耦"的单轴评估。

### 5. 从 run 到结论：数据流与分析

```
sweep.py ──► 130 个 run ──► 每 run: log.csv + metrics.json
                              │
              metrics.aggregate ▼
                    summary.csv (每配置 mean/std/n, 13 行)
                    summary_runs.csv (每 rep 原始值, 130 行)
                              │
              analysis.build_outputs ▼
                    sensitivity_table.md     ← 核心交付物
                    step_response_overlay.png ← 均值曲线 + ±std 带
                    sensitivity_plots.png     ← 5 点拟合 + 误差棒
```

- **6 个标量指标**（`metrics.py`），覆盖"快、准、稳、综合"四类：

  | 指标 | 含义 | 主要受影响的参数 |
  |---|---|---|
  | `rise_time_10_90` | 误差从 90%→10% 阶跃量的时间（多快接近） | $K_p$（强） |
  | `peak_time` | 首次到达极值的时刻 | $K_p$ |
  | `overshoot_pct` | 超调百分比（冲过头多少） | $K_p\!\uparrow$, $K_d\!\downarrow$ |
  | `settling_time_5pct` | 误差进入 $\pm5\%$ 带且不再出去的时刻（多久彻底稳） | $K_d$（强） |
  | `steady_state_error` | 末段 5 s 平均 $|e|$（长期偏差） | $K_i$（强） |
  | `iae` | $\int|e|\,dt$，综合质量分（通常对参数呈 U 型） | 综合 |

- **敏感性量化**（`analysis.compute_sensitivity`）：对每个（指标 × 参数）组合，取 5 个档位的**均值点** $\{-20,-10,0,+10,+20\%\}$，一阶 `polyfit` 得三个数——
  - **slope**：斜率，单位 = 指标单位 / 100% 参数变化，给出灵敏度**强度与方向**；
  - **range**：5 点 max−min，给出 $\pm20\%$ 内的**实际变化幅度**；
  - **R²**：5 点对直线的决定系数，$\to1$ 表示单调线性，偏小表示非线性（如 IAE 的 U 型）。

- **可视化呈现散布**：叠加图每档画 10 次重采样后的**均值曲线 + $\pm$std 阴影带**；散点图每个均值点带**$\pm$std 误差棒**；表格 Nominal 列以 `mean (±std)` 呈现。

### 6. 鲁棒性与验收

- **错误处理**：HTTP 失败重试 2 次；复位超出容差（航向 $\pm5°$/高度 $\pm50$ ft）抛 `ResetVerificationError`；单 run 异常被捕获记入 `failed.json` 并跳过，累计失败 $>\max(3,\,\text{总run数}//10)$ 才中止整个 sweep（130-run 容忍 $\le13$ 个零散失败）。
- **验收标准**：130 run 基本跑完；产出文件齐备；Nominal 响应曲线形态正常；且至少满足教科书结论——$K_p$ 对 rise_time/overshoot 的 R²>0.7、$K_i$ 对 steady_state_error 的 R²>0.7。

---

## 第二部分　论文写作（实验目的、设计与原理）

### 实验目的、设计与原理

为定量刻画 PID 控制律的三个超参数（比例增益 $K_p$、积分增益 $K_i$、微分增益 $K_d$）对飞行器单轴阶跃响应控制品质的影响，本研究在 Microsoft Flight Simulator 2024 高保真仿真环境中构建了一套超参数敏感性评估实验台。该实验台从原视觉伺服控制系统中剥离出纯 PID 反馈回路，以仿真直接输出的航向角与气压高度作为状态反馈量，取代原有的视觉目标跟踪测量，从而消除感知噪声对控制评估的干扰。单次实验为一次单轴阶跃响应：控制器在每个采样周期读取被控量、按真实循环间隔更新 PID 输出，并经一组幅值固定、时长可调的离散（bang-bang）舵面指令作用于仿真飞行器，构成"测量—决策—执行—被控对象—再测量"的完整闭环；其中航向通道经方向舵、高度通道经升降舵实现控制，二者作用于物理正交的操纵面。

实验采用一次一因子（OFAT）设计：以原系统标定值为基准点（nominal），令每个超参数分别在 $\pm10\%$ 与 $\pm20\%$ 四个档位上单独偏移、其余两参数固定，单轴共 13 个配置，专注于辨识各参数的独立主效应而不引入交互项，以换取结论的可读性与可归因性。为隔离双轴间的气动与姿态耦合、保证主效应的纯净辨识，每次实验仅激活并扫描一个通道，另一通道被复位至固定状态并令其操纵面保持中立，且评估指标只统计被激活通道；航向与高度因而作为两组完全独立的扫描分别执行。鉴于系统固有的随机性（通信节拍抖动、bang-bang 控制的稳态颤振、以及复位容差导致的真实阶跃幅度波动），每个配置重复 10 次，单轴累计 130 次试验。

对每次试验,我们从响应日志中计算上升时间、峰值时间、超调量、调节时间、稳态误差与误差绝对值积分（IAE）共六项标量指标,分别表征响应的速度、精度、稳定性与综合品质。敏感性量化建立在每个配置 10 次重复的均值之上:对每一(指标,参数)组合,以五个档位的均值点作一阶线性拟合,报告斜率(以"每 100% 参数变化"归一,反映灵敏度的强度与方向)、极差(反映 $\pm20\%$ 范围内的实际变化幅度)与决定系数 $R^2$(反映该影响的单调线性程度);重复间的标准差则以误差棒、阴影带及表内 $\text{mean}\pm\text{std}$ 的形式一并呈现,以量化结论的可信度。该设计在真实仿真动力学下系统地给出了三个 PID 超参数对各项控制指标影响的方向、强度与线性度,为后续的控制器整定提供了实证依据。
